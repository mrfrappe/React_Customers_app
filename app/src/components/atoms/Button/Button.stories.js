import React from 'react';
import Button from './Button';

export default { title: 'Atoms/Button' };

export const Primary = () => <Button>Button</Button>;

export const Delete = () => (<Button secondary>Button</Button>);